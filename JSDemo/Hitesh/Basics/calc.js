// ; is optional

let score = 100

score = 120;

const bonus = 15;

let finalScore = (score + bonus) * 1.8;


console.log("FinalScore: " + finalScore);


let tempInF = 100
let tempInC = (tempInF - 32) * (5 / 9)
console.log(tempInF + " in F is equal to " + tempInC + " in C");